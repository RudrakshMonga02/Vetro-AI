# Backend API base URL. Defaults to http://localhost:8000 if unset
# (see src/lib/apiClient.js) -- only needs to be set explicitly for a
# real deployment where the frontend and backend aren't both localhost.
VITE_API_BASE=http://localhost:8000
